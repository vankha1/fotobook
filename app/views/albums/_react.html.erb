<%= form_with(url: albums_reactions_path, data: { remote: true }) do |f| %>
    <div><%= hidden_field_tag :album_id, album.id %></div>
    <%= button_tag type: 'submit', class: "fa fa-heart p-2 text-secondary", style: "background-color: transparent; border: none", id: "react_btn" do %>
        &nbsp;<span id="number_of_like"><%= album.number_like %></span>
    <% end %>
<% end %>